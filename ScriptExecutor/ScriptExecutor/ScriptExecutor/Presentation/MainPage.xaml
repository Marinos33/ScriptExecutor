<Page x:Class="ScriptExecutor.Presentation.MainPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="using:ScriptExecutor.Presentation" xmlns:uen="using:Uno.Extensions.Navigation.UI" xmlns:utu="using:Uno.Toolkit.UI" xmlns:um="using:Uno.Material" xmlns:icons="using:System.Windows.Controls" NavigationCacheMode="Required" Background="{ThemeResource BackgroundBrush}">
    <ScrollViewer>
        <Grid utu:SafeArea.Insets="VisibleBounds">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <utu:NavigationBar Content="ScriptExecutor" />
            <!-- Table Header -->
            <Grid Grid.Row="2" Margin="20,0,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <!-- Additional rows will be added dynamically -->
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="150" />
                </Grid.ColumnDefinitions>
                <!-- Header Row -->
                <Border Grid.Row="0" Grid.ColumnSpan="3" Background="Purple" Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="150" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Process Name" FontWeight="Bold" Foreground="White" />
                        <TextBlock HorizontalAlignment="Center" Grid.Column="1" Text="Status" FontWeight="Bold" Foreground="White" />
                        <TextBlock HorizontalAlignment="Center" Grid.Column="2" Text="Actions" FontWeight="Bold" Foreground="White" />
                    </Grid>
                </Border>
                <!-- Sample Row (you would typically bind this to a collection) -->
                <Border Grid.Row="1" Grid.ColumnSpan="3" BorderBrush="{ThemeResource SystemBaseLowColor}" BorderThickness="1,0,1,1" Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="150" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Sample Script" VerticalAlignment="Center" />
                        <SymbolIcon Grid.Column="1" Symbol="Accept" Foreground="Green" VerticalAlignment="Center" />
                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="10">
                            <Button Background="Transparent" UseLayoutRounding="False" ToolTipService.ToolTip="Edit">
                                <SymbolIcon ManipulationMode="None" Symbol="Edit" Foreground="BlueViolet" />
                            </Button>
                            <Button Background="Transparent" ToolTipService.ToolTip="Remove">
                                <SymbolIcon Symbol="Delete" Foreground="BlueViolet" />
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
                <!-- You would typically use an ItemsControl or ListView bound to a collection -->
                <!-- For example:
        <ItemsControl Grid.Row="1" ItemsSource="{Binding ScriptItems}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              ... template similar to the sample row above ...
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        -->
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
