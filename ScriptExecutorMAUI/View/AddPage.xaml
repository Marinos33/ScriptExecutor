<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ScriptExecutorMAUI.View.AddPage"
             xmlns:model="clr-namespace:ScriptExecutorMAUI.DTOModel"
             xmlns:viewmodel="clr-namespace:ScriptExecutorMAUI.ViewModel"
             x:DataType="viewmodel:AddPageViewModel"
             Title="Add a program"
             BackgroundColor="Black">
    <VerticalStackLayout 
        Padding="24" 
        Margin="20,50,20,50" 
        HeightRequest="850"
        WidthRequest="600"
        BackgroundColor="#292929">
        <VerticalStackLayout>
            <Label 
                Text="Name of the profile"/>
            <Entry 
                BackgroundColor="White" 
                TextColor="Black"
                Text="{Binding Name}"
                HeightRequest="40"/>
        </VerticalStackLayout>
        <VerticalStackLayout 
            Margin="0,40,0,0">
            <Label 
                Text="Name of the executable" />
            <HorizontalStackLayout>
                <Entry 
                    x:Name="ExecutableEntry"
                    BackgroundColor="White" 
                    TextColor="Black" 
                    Text="{Binding ExecutableFile}"
                    WidthRequest="350" />
                <Button 
                    x:Name="PickExe"
                    ImageSource="index.png"  
                    WidthRequest="70" 
                    HeightRequest="60" 
                    BorderColor="Black"  
                    HorizontalOptions="Center" 
                    BackgroundColor="#5A5A5A"
                    Margin="20,0,0,0"
                    ContentLayout="Left,0"  />
            </HorizontalStackLayout>
        </VerticalStackLayout>
        <VerticalStackLayout Margin="0,40,0,0">
            <Label 
                Text="Script to run" />
            <Editor 
                BackgroundColor="White" 
                TextColor="#030000" 
                AutoSize="TextChanges" 
                Text="{Binding Script}"
                HeightRequest="200" />
            <Button 
                Text="run script"
                HorizontalOptions="End" 
                Margin="0,10,0,0" 
                BorderColor="Black" 
                TextColor="Black"/>
        </VerticalStackLayout>
        <Frame 
            Margin="0,20,0,0" 
            WidthRequest="300" 
            HorizontalOptions="Start" 
            Padding="8">
            <VerticalStackLayout  BackgroundColor="#5A5A5A" Padding="15">
                <Label 
                Text="When to run the script" />
                <HorizontalStackLayout Margin="0,20,0,0">
                    <CheckBox 
                    VerticalOptions="Center"
                    IsChecked="{Binding RunAfterShutdown}"/>
                    <Label 
                    Text="After Shutdown" 
                    VerticalOptions="Center"/>
                </HorizontalStackLayout>
                <HorizontalStackLayout>
                    <CheckBox 
                    VerticalOptions="Center"
                        IsChecked="{Binding RunOnStart}" />
                    <Label 
                    Text="On Launch" 
                    VerticalOptions="Center"/>
                </HorizontalStackLayout>
            </VerticalStackLayout>
        </Frame>
        <Button Text="Add" 
                BorderColor="Black" 
                TextColor="Black" 
                HorizontalOptions="Center" 
                VerticalOptions="End"
                Margin="0,50,0,0"
                Command="{Binding AddProcessCommand}"
                FontSize="Large"/>
    </VerticalStackLayout>
</ContentPage>